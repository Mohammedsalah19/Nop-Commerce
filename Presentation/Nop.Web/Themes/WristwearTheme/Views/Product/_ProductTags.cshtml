@model IList<ProductTagModel>
@using Nop.Web.Models.Catalog;
    @if (Model.Count > 0)
    {
        <div class="product-tags-box">
            @*<div class="title panel-heading">
                <h2>@T("Products.Tags")</h2>
            </div>*@
            <div class="product-tags-list">
                <ul class="nav nav-pills">
                    @for (int i = 0; i < Model.Count; i++)
                    {
                        <li class="label label-default">
                            <a href="@Url.RouteUrl("ProductsByTag", new { productTagId = Model[i].Id, SeName = Model[i].SeName })" class="label producttag">
                                @Model[i].Name<span> @T("Products.Tags.Count", Model[i].ProductCount)</span>
                            </a>

                        </li>
                        if (i != Model.Count - 1)
                        {
                            <li class="separator">, </li>
                        }
                    }
                </ul>
            </div>
        </div>

    }
